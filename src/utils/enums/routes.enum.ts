export enum Routes {
  // Public
  SIGN_IN = 'Login',
  SIGN_UP = 'sign_up',

  // Private
  HOME = 'home',
  TRADE = 'trade',
  PORTFOLIO = 'portfolio',
  DETAILS = 'details'

}

export enum Stacks {
  PUBLIC = 'public',
  PRIVATE = 'private',
  APP_TABS = 'app_tabs'
}
